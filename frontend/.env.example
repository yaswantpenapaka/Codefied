# Dev: use Vite proxy (cookies work without localStorage)
VITE_API_URL=/api
VITE_APP_URL=http://localhost:5173

# Set in Vercel dashboard (Settings → Environment Variables) to disable Vercel Toolbar:
# VERCEL_TOOLBAR=0