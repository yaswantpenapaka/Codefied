services:
  mongo:
    image: mongo:7
    container_name: codefied-mongo
    profiles: ["local-db"]
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

  redis:
    image: redis:7-alpine
    container_name: codefied-redis
    ports:
      - "127.0.0.1:6379:6379"
    command:
      [
        "redis-server",
        "--maxmemory",
        "64mb",
        "--maxmemory-policy",
        "allkeys-lru",
        "--save",
        "",
        "--appendonly",
        "no",
      ]
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 96M

  # Optional — production on t3.small uses PM2 + nginx instead (see scripts/setup-t3-small.sh)
  backend:
    profiles: ["docker"]
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: codefied-backend
    depends_on:
      - redis
    env_file:
      - .env
    environment:
      NODE_ENV: production
      PORT: 4000
      REDIS_HOST: redis
      REDIS_PORT: 6379
      NODE_OPTIONS: --max-old-space-size=384
    ports:
      - "4000:4000"
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512M
        reservations:
          memory: 256M

volumes:
  mongo-data: